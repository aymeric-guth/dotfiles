find `echo $PROJECTS` -mindepth 2 -maxdepth 2 -type d -iname .git -print0 | xargs -n1 -0 /bin/sh -c 'cd $(dirname $1) && [ ! $(git status --porcelain=v1 2>/dev/null | wc -l) -eq 0 ] && echo $(dirname $1)' ''
