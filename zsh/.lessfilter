#! /usr/bin/env sh
# this is a example of .lessfilter, you can change it
mime=$(file -bL --mime-type "$1")
# category=${mime%%/*}
# kind=${mime##*/}
if [ -d "$1" ]; then
	ls -lha "$1"
elif [ "$mime" = text/plain ]; then
	cat --number "$1"
else
	nop
fi
